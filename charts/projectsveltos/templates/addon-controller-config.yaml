apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "projectsveltos.fullname" . }}-addon-controller-config
  labels:
  {{- include "projectsveltos.labels" . | nindent 4 }}
data:
  controller_manager_config.yaml: {{ .Values.addonControllerConfig.controllerManagerConfigYaml
    | toYaml | indent 1 }}