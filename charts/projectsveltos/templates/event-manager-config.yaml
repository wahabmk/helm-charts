apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "projectsveltos.fullname" . }}-event-manager-config
  labels:
  {{- include "projectsveltos.labels" . | nindent 4 }}
data:
  controller_manager_config.yaml: {{ .Values.eventManagerConfig.controllerManagerConfigYaml
    | toYaml | indent 1 }}