apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "projectsveltos.fullname" . }}-addon-controller-rolebinding-extra
  labels:
  {{- include "projectsveltos.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: '{{ include "projectsveltos.fullname" . }}-addon-controller-role-extra'
subjects:
- kind: ServiceAccount
  name: '{{ include "projectsveltos.fullname" . }}-addon-controller'
  namespace: '{{ .Release.Namespace }}'