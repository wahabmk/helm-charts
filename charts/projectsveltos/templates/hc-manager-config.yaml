apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "projectsveltos.fullname" . }}-hc-manager-config
  labels:
  {{- include "projectsveltos.labels" . | nindent 4 }}
data:
  controller_manager_config.yaml: {{ .Values.hcManagerConfig.controllerManagerConfigYaml
    | toYaml | indent 1 }}